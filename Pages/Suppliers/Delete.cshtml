@page
@model InventorySystem.Pages.Suppliers.DeleteModel
@{
    ViewData["Title"] = "Delete Supplier";
}

<div class="row">
    <div class="col-md-6 offset-md-3">
        <div class="card border-danger">
            <div class="card-header bg-danger text-white">
                <h3><i class="bi bi-trash"></i> Delete Supplier</h3>
            </div>
            <div class="card-body">
                <div class="alert alert-warning">
                    <i class="bi bi-exclamation-triangle"></i> Are you sure you want to delete this supplier?
                    @if (Model.Supplier.Products.Any())
                    {
                        <br><strong>Warning: This supplier has @Model.Supplier.Products.Count product(s) associated with it!</strong>
                    }
                </div>

                <dl class="row">
                    <dt class="col-sm-4">Name:</dt>
                    <dd class="col-sm-8">@Model.Supplier.Name</dd>

                    <dt class="col-sm-4">Email:</dt>
                    <dd class="col-sm-8">@Model.Supplier.Email</dd>

                    <dt class="col-sm-4">Phone:</dt>
                    <dd class="col-sm-8">@Model.Supplier.Phone</dd>

                    <dt class="col-sm-4">Address:</dt>
                    <dd class="col-sm-8">@Model.Supplier.Address</dd>

                    <dt class="col-sm-4">Products:</dt>
                    <dd class="col-sm-8">@Model.Supplier.Products.Count</dd>
                </dl>

                <form method="post">
                    <input type="hidden" asp-for="Supplier.Id" />
                    <div class="d-flex justify-content-between">
                        <a href="/Suppliers" class="btn btn-secondary">
                            <i class="bi bi-arrow-left"></i> Cancel
                        </a>
                        <button type="submit" class="btn btn-danger">
                            <i class="bi bi-trash"></i> Delete
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
